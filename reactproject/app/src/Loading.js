import React from 'react'

//here we can use 'props' and {props.var} or use {var} with the same same at the other side
const ImportLoading = ({text}) => <div> <h3>Loading {text} - imported component from Loading.js</h3></div>; //we can use export 

export default ImportLoading
